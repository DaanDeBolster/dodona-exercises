<!-- Een **bitstring** is een string van karakters die enkel `0` of `1` kan bevatten Het **Hamminggewicht** van een bitstring is het aantal karakters dat niet `0` is.

Schrijf een programma dat een bitstring als input neemt en het Hamminggewicht van de bitstring teruggeeft.

## Example

```console?lang=python&prompt=>>>
>>> echo("Hello world!")
"Hello world!"
>>> echo(5)
5
``` -->
